<UserControl x:Class="EliteWhisper.Views.Pages.LocalModelsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:EliteWhisper.ViewModels"
             xmlns:converters="clr-namespace:EliteWhisper.Converters"
             mc:Ignorable="d" 
             x:Name="PageRoot"
             d:DesignHeight="700" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="InverseBoolToVis" Invert="True"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32">
            
            <TextBlock Text="Local Model Hub" Style="{StaticResource HeadingLarge}" Margin="0,0,0,8"/>
            <TextBlock Text="Manage and run offline AI models using built-in llama.cpp." Style="{StaticResource BodyText}" Margin="0,0,0,24"/>

            <!-- Status Bar -->
            <Border Background="{StaticResource BackgroundSecondary}" CornerRadius="8" Padding="12" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    
                    <ProgressBar Grid.Column="1" Value="{Binding DownloadProgress}" Maximum="100" Height="10" Margin="16,0" 
                                 Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    
                    <Button Grid.Column="2" Content="Cancel" Command="{Binding CancelDownloadCommand}" 
                            Style="{StaticResource SecondaryButton}" 
                            Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>
            </Border>

            <TabControl Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Margin="0,0,16,0" Padding="12,8">
                                        <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            Margin="12,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- INSTALLED MODELS -->
                <TabItem Header="Installed">
                    <StackPanel Margin="0,16,0,0">
                        <ItemsControl ItemsSource="{Binding InstalledModels}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="{Binding Size}" Foreground="{StaticResource TextSecondary}" Margin="0,0,12,0"/>
                                                    <TextBlock Text="Ready" Foreground="{StaticResource AccentSuccess}"/>
                                                </StackPanel>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <Button Content="Benchmark" Command="{Binding DataContext.BenchmarkCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                                                <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource DestructiveButton}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <TextBlock Text="No models installed. Go to the 'Available' tab to download one." 
                                   Visibility="{Binding InstalledModels.Count, Converter={StaticResource InvertedCountToVisibilityConverter}}"
                                   Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,32"/>
                    </StackPanel>
                </TabItem>

                <!-- AVAILABLE MODELS -->
                <TabItem Header="Available">
                    <StackPanel Margin="0,16,0,0">
                         <ItemsControl ItemsSource="{Binding AvailableModels}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel>
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="16"/>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,4,0,8" Foreground="{StaticResource TextSecondary}"/>
                                                
                                                <ItemsControl ItemsSource="{Binding Tags}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Orientation="Horizontal"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{StaticResource BackgroundTertiary}" CornerRadius="4" Padding="6,2" Margin="0,0,6,0">
                                                                <TextBlock Text="{Binding}" FontSize="11"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" MinWidth="140">
                                                <TextBlock Text="{Binding SizeGb, StringFormat={}{0} GB}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding RecommendedRamGb, StringFormat={}Req RAM: {0} GB}" HorizontalAlignment="Right" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,8"/>
                                                
                                                <!-- Download Button -->
                                                <Button Content="Download" 
                                                        Command="{Binding DataContext.DownloadCommand, ElementName=PageRoot}" 
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource PrimaryButton}" 
                                                        Width="120"
                                                        HorizontalAlignment="Right"
                                                        Visibility="{Binding IsDownloading, Converter={StaticResource InverseBoolToVis}}"/>
                                                
                                                <!-- Progress Display -->
                                                <StackPanel Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <Grid Margin="0,0,0,4">
                                                        <TextBlock Text="Downloading..." FontSize="11" Foreground="{StaticResource TextSecondary}"/>
                                                        <TextBlock Text="{Binding DownloadProgress, StringFormat={}{0:N0}%}" HorizontalAlignment="Right" FontSize="11" FontWeight="SemiBold"/>
                                                    </Grid>
                                                    <ProgressBar Value="{Binding DownloadProgress}" Maximum="100" Height="6" Width="120" HorizontalAlignment="Right"
                                                                 Background="{StaticResource BackgroundTertiary}" Foreground="{StaticResource AccentPrimary}" BorderThickness="0"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </TabItem>

                <!-- RUNTIME SETTINGS -->
                <TabItem Header="Settings">
                    <StackPanel Margin="0,16,0,0">
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="Runtime Configuration" Style="{StaticResource HeadingSmall}" Margin="0,0,0,16"/>
                                
                                <TextBlock Text="Model Storage Path" Margin="0,0,0,4"/>
                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding LocalModelsPath}" IsReadOnly="True" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseModelsPathCommand}" Style="{StaticResource SecondaryButton}"/>
                                </Grid>

                                <TextBlock Text="CPU Threads" Margin="0,0,0,4"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                    <Slider Width="200" Minimum="1" Maximum="32" Value="{Binding ThreadCount}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                                    <TextBlock Text="{Binding ThreadCount}" VerticalAlignment="Center" Margin="12,0,0,0" Width="30"/>
                                </StackPanel>

                                <TextBlock Text="Context Size (Tokens)" Margin="0,0,0,4"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                    <ComboBox SelectedValue="{Binding ContextSize}" SelectedValuePath="Tag" Width="100" HorizontalAlignment="Left">
                                        <ComboBoxItem Content="2048" Tag="2048"/>
                                        <ComboBoxItem Content="4096" Tag="4096"/>
                                        <ComboBoxItem Content="8192" Tag="8192"/>
                                    </ComboBox>
                                </StackPanel>

                                <TextBlock Text="Temperature (Creativity)" Margin="0,0,0,4"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                                    <Slider Width="200" Minimum="0" Maximum="1.5" Value="{Binding Temperature}" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                                    <TextBlock Text="{Binding Temperature, StringFormat={}{0:N1}}" VerticalAlignment="Center" Margin="12,0,0,0" Width="30"/>
                                </StackPanel>

                                <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </TabItem>
            </TabControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
